1-	Provide Execute Permission to both files.
chmod +x ocs-install.sh
chmod +x ocs-uninstall.sh
2-	Run Script manually
Sudo bash ocs-uninstall.sh
Sudo bash ocs-install.sh
wait untill the cmd completes itself.
OCS is installed successfully
